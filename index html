<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All CMD Commands</title>
  <style>
    body {
      background: radial-gradient(ellipse at bottom, #0d0d1a 0%, #000 100%);
      color: #eee;
      font-family: Arial, sans-serif;
      padding: 2em;
      min-height: 100vh;
      overflow-x: hidden;
      margin: 0;
      position: relative;
    }

    /* Galaxy effect */
    body::before {
      content: "";
      position: fixed;
      top: 0; left: 0; width: 100%; height: 100%;
      background:
        radial-gradient(circle at 20% 30%, rgba(255,0,150,0.3) 0%, transparent 60%),
        radial-gradient(circle at 80% 70%, rgba(0,200,255,0.3) 0%, transparent 60%),
        radial-gradient(circle at 50% 90%, rgba(0,255,150,0.25) 0%, transparent 60%);
      z-index: 0;
      pointer-events: none;
    }

    /* Neon glowing edge */
    body::after {
      content: "";
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      border: 6px solid #ff00ff;
      box-shadow: 
        0 0 15px #ff00ff,
        0 0 30px #ff00ff,
        0 0 60px #ff33ff;
      border-radius: 15px;
      z-index: 9999;
      pointer-events: none;
    }

    h1 {
      text-align: center;
      color: #39ff14;
      text-shadow: 0 0 10px #39ff14, 0 0 20px #00e6ff;
      margin-bottom: 0.5em;
      position: relative;
      z-index: 2;
    }

    /* Clock + Date */
    #datetime {
      text-align: center;
      font-size: 1.3em;
      color: #00e6ff;
      text-shadow: 0 0 8px #00e6ff, 0 0 15px #39ff14;
      margin-bottom: 2em;
      position: relative;
      z-index: 2;
    }

    .section-title {
      color: #00e6ff;
      margin-top: 2em;
      border-bottom: 2px solid #00e6ff;
      padding-bottom: 0.2em;
      text-shadow: 0 0 8px #00e6ff;
      position: relative;
      z-index: 2;
    }

    .command {
      background: rgba(0, 0, 0, 0.6);
      border-left: 4px solid #ff00ff;
      padding: 0.5em 1em;
      margin: 0.5em 0;
      border-radius: 8px;
      box-shadow: 0 0 10px #ff00ff;
      font-size: 1.1em;
      position: relative;
      z-index: 2;
    }

    .command b {
      color: #ff00ff;
      text-shadow: 0 0 8px #ff00ff, 0 0 15px #ff33ff;
    }

    .command span {
      color: #eee;
    }

    /* Programming Languages List */
    .languages {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 1em;
      margin-top: 2em;
      position: relative;
      z-index: 2;
    }

    .lang {
      text-align: center;
      padding: 1em;
      border-radius: 12px;
      font-weight: bold;
      font-size: 1.1em;
      cursor: pointer;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .lang:hover {
      transform: scale(1.1);
      box-shadow: 0 0 20px #fff;
    }

    .lang.python { color: #ffdd00; text-shadow: 0 0 8px #ffdd00; border: 2px solid #ffdd00; }
    .lang.javascript { color: #ffcc00; text-shadow: 0 0 8px #ffcc00; border: 2px solid #ffcc00; }
    .lang.java { color: #ff4500; text-shadow: 0 0 8px #ff4500; border: 2px solid #ff4500; }
    .lang.cpp { color: #00bfff; text-shadow: 0 0 8px #00bfff; border: 2px solid #00bfff; }
    .lang.csharp { color: #9932cc; text-shadow: 0 0 8px #9932cc; border: 2px solid #9932cc; }
    .lang.php { color: #6a5acd; text-shadow: 0 0 8px #6a5acd; border: 2px solid #6a5acd; }
    .lang.ruby { color: #dc143c; text-shadow: 0 0 8px #dc143c; border: 2px solid #dc143c; }
    .lang.go { color: #00ced1; text-shadow: 0 0 8px #00ced1; border: 2px solid #00ced1; }

    footer {
      text-align:center;
      margin-top:3em;
      padding:1em;
      color:#ff00ff;
      font-size:1em;
      font-weight: bold;
      text-shadow: 0 0 8px #ff00ff, 0 0 15px #ff33ff;
      position: relative;
      z-index: 2;
    }
  </style>
</head>
<body>
  <h1>All CMD Commands</h1>

  <!-- Live Date & Time -->
  <div id="datetime">Loading time...</div>

  <!-- Example Commands -->
  <h2 class="section-title">Navigation & File Management</h2>
  <div class="command"><b>dir</b> <span>— List files and directories</span></div>
  <div class="command"><b>cd</b> <span>— Change directory</span></div>
  <div class="command"><b>copy</b> <span>— Copy files</span></div>

  <!-- Programming Languages Section -->
  <h2 class="section-title">Programming Languages</h2>
  <div class="languages">
    <div class="lang python">🐍 Python</div>
    <div class="lang javascript">⚡ JavaScript</div>
    <div class="lang java">☕ Java</div>
    <div class="lang cpp">💠 C++</div>
    <div class="lang csharp">#️⃣ C#</div>
    <div class="lang php">🐘 PHP</div>
    <div class="lang ruby">💎 Ruby</div>
    <div class="lang go">🚀 Go</div>
  </div>

  <footer>
    🚀 Web Development by Darlington Tech
  </footer>

  <script>
    function updateDateTime() {
      const now = new Date();
      const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
      const date = now.toLocaleDateString(undefined, options);
      const time = now.toLocaleTimeString();
      document.getElementById('datetime').textContent = `🕒 ${time} | 📅 ${date}`;
    }
    setInterval(updateDateTime, 1000);
    updateDateTime();
  </script>
</body>
</html>
